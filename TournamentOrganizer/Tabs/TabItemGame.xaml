<TabItem x:Class="TournamentOrganizer.UI.Tabs.TabItemGame" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      Header="Game">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="AllGames" Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="9*" />
        </Grid.RowDefinitions>
        <DataGrid IsEnabled="{Binding StateDataGrid}" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" x:Name ="DataGridListGames" Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Games}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="2*" Header="Name" Binding="{Binding Name}"/>
                <DataGridTemplateColumn Width="1*" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ButtonGameEdit_Click">Редактировать</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="1*" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ButtonGameDelite_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >Удалить</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox  Text="{Binding TextBoxAddGameNameText}" TextChanged="TextBoxAddGameName_TextChanged" x:Name="TextBoxAddGameName"/>
            <Button x:Name="ButtonAddEdit" Click="ButtonAdd_Click" Content="{Binding ButtonAddEditContent}"/>
            <Button Visibility="{Binding VisibilityButtonCancel}" IsEnabled="{Binding StateButtonCancel}" x:Name="ButtonCancel" Content="Отмена" Click="ButtonCancel_Click"/>
        </StackPanel>

    </Grid>
</TabItem>